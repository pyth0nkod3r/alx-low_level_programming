#include <stdio.h>

/**
* main - program start
* Return: Zero
*/


int main(void)
{
char x, y;
for (x = 48; x < 58 ; x++)
{
putchar(x);
}
for (y = 97; y < 103; y++)
{
putchar(y);
}
putchar(10);
return (0);
}
